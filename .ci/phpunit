#!/usr/bin/env bash
set -e

export PHPUNIT_OUTPUT_DIR=build/ci/phpunit
rm -rf "$PHPUNIT_OUTPUT_DIR" && mkdir -p "$PHPUNIT_OUTPUT_DIR"
vendor/bin/phpunit --configuration phpunit.xml.dist --log-junit "$PHPUNIT_OUTPUT_DIR/junit.xml"
