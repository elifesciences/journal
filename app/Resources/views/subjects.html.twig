{% extends '::page.html.twig' %}

{% block title %}Research categories{% endblock %}

{% block info_bar %}

    {{ info_bar('This page is incomplete.') }}

{% endblock %}

{% block body %}

    {{ render_pattern(contentHeader) }}

    <div class="wrapper blg-pad-top-medium">

        {% if subjects %}

            {{ render_pattern(subjects) }}

        {% else %}

            <p>No subjects available.</p>

        {% endif %}

    </div>

{% endblock %}
