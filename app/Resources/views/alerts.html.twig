{% extends '::page.html.twig' %}

{% block info_bar %}

    {{ info_bar('This page is incomplete.') }}

{% endblock %}

{% block body %}

    {{ render_pattern(contentHeader) }}

    <div class="wrapper u-pad-top-lg">

        <div class="grid">

            <div class="{% include 'grid-item-classes.html.twig' %}">

                {% for part in body %}

                    {{ render_pattern(part) }}

                {% endfor %}

            </div>

        </div>

    </div>

{% endblock %}
