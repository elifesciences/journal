{% extends '::page.html.twig' %}

{% block body %}

    {{ render_pattern(contentHeader) }}

    {% if leadParas %}

        <div class="wrapper">
            {{ render_pattern(leadParas) }}
        </div>

    {% endif %}

    <div class="wrapper u-pad-top-lg">

        <div class="grid">

            <div class="listing-column-container">

                <div class="grid__item large--eight-twelfths listing-column-main {% if not highlights %}push--large--two-twelfths{% endif %}">

                    {{ render_pattern(listing) }}

                </div>

                {% if highlights %}

                    <div class="grid__item large--four-twelfths listing-column-secondary">

                        {{ render_pattern(highlights) }}

                    </div>

                {% endif %}

            </div>

        </div>

    </div>

{% endblock %}
