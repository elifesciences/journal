{% extends '::page.html.twig' %}

{% block title %}{{ title|striptags }} | Collections{% endblock %}

{% block description %}
    {{ collection.impactStatement|raw }}
{% endblock %}

{% block body %}

    {{ render_pattern(contentHeader) }}

    {% embed 'grid/listing-two-column.html.twig' %}

        {% block main %}

            {% for part in body %}

                {{ render_pattern(part) }}

            {% endfor %}

        {% endblock %}

        {% block secondary %}

            {% if multimedia %}

                {{ render_pattern(multimedia) }}

            {% endif %}

            {% if related %}

                {{ render_pattern(related) }}

            {% endif %}

            {{ render_pattern(contributors) }}

        {% endblock %}

    {% endembed %}

{% endblock %}
