{% extends '::page.html.twig' %}

{% block title %}Labs{% endblock %}

{% block info_bar %}

    {{ info_bar('This page is incomplete.') }}

{% endblock %}

{% block body %}

    {{ render_pattern(contentHeader) }}

    <div class="wrapper">

        {{ render_pattern(leadParas) }}

        {% if experiments %}

            {{ render_pattern(experiments) }}

        {% else %}

        <p class="u-pad-top-lg">No experiments available.</p>

        {% endif %}

    </div>

{% endblock %}
