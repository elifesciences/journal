{% set hypothesis = true %}

{% extends '::page.html.twig' %}

{% block title %}{{ title|striptags }} | Inside eLife{% endblock %}

{% block body %}

    {{ render_pattern(contentHeader) }}

    {% if contextualData %}

        <div class="wrapper">

            {{ render_pattern(contextualData) }}

        </div>

    {% endif %}

    {% embed 'grid/content.html.twig' %}

        {% block main %}

            {% if hypothesisOpener is defined %}
                {{ render_pattern(hypothesisOpener) }}
            {% endif %}

            {% for block in blocks %}
                {{ render_pattern(block) }}
            {% endfor %}

        {% endblock %}

    {% endembed %}

{% endblock %}
