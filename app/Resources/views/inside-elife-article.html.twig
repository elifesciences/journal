{% set criticalCss = 'post' %}

{% set hypothesis = true %}
{% set hypothesis = true %}
{% set customWrapperClass = 'content--narrow-column' %}

{% extends '::page.html.twig' %}

{% block title %}{{ title|striptags }} | Inside eLife{% endblock %}

{% block body %}

    <style>
        #content {
            padding-top: 48px;
            padding-top: 3rem;
        }
    </style>

    {{ render_pattern(contentHeader) }}

    {% include 'calls-to-action.html.twig' %}

    {% embed 'grid/content-new.html.twig' %}

        {% block main %}

            {% for block in blocks %}
                {{ render_pattern(block) }}
            {% endfor %}

        {% endblock %}

    {% endembed %}

    {{ schema_org_metadata(item) }}

{% endblock %}
